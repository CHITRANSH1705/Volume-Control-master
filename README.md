# Volume-Control-master
A Chrome (Manifest V3) extension that gives every open tab its own volume control — independent of the OS mixer and independent of each other. v2.0 is a full rewrite of the original proof of concept. The old version could only turn a tab down (native `&lt;video>`/`&lt;audio>` volume caps at 100%) and forgot every setting the moment the popup closed. 
